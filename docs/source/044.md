### 归并排序

   * 归并排序是基于分治思想的排序方法，将待排序数据不断划分为子序列，最后再将子序列不断拼合，在拼合中保证持续有序，最后实现整体有序。

   * 归并排序分为两步：1. 分解 2. 合并，在合并中如何保证子序列有序拼合是解决问题的关键。

   * 合并：建立空数组存放结果，对于偶数对，判断子序列之间的大小关系，添加较小的值，对于奇数（孤立）对则直接添加。

---

### 代码实现

    def merge_sort(data):
        if len(data) <= 1:
            return data
        middle = len(data) // 2
        # 分割成左右两部分
        left, right = data[0:middle], data[middle:]
        # 递归进行分割，最后合并
        return merge(merge_sort(left), merge_sort(right))

    # 拼接函数
    def merge(left,right):
        # 存储结果
        result = []
        while left and right:
            # 向结果中添加较小元素
            if left[0] <= right[0]:
                result.append(left.pop(0))
            else:
                result.append(right.pop(0))
        while left:
            result.append(left.pop(0))
        while right:
            result.append(right.pop(0))
        return result

---

### 性能分析

   * 时间复杂度：O(nlogn)
   * 空间复杂度：O(n)

---

### 关键点

   1. 递归时的左右值选取
   2. 奇数情况的处理
